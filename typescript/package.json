{
  "name": "typescript-extension-test",
  "version": "1.0.0",
  "description": "Trying to figure out I'm going to extend TypeScript classes.",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "clean": "rm -rf build/*",
    "build": "tsc",
    "clean-build": "rm -rf build/* && tsc",
    "build:watch": "tsc --watch",
    "clean-build:watch": "rm -rf build/* && tsc --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://ashleydavis@github.com/ashleydavis/typescript-extension-test.git"
  },
  "keywords": [],
  "author": "ashley@codecapers.com.au",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "8.0.28",
    "ts-node": "3.3.0",
    "typescript": "2.5.2"
  }
}
